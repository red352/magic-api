interface:
  display_name: "magic-api 工作区"
  short_description: "通过统一 CLI 自动管理、同步和验证 magic-api 工作区"
  default_prompt: "Use $magic-api-workspace to plan, apply, synchronize, and verify the requested magic-api workspace changes."
